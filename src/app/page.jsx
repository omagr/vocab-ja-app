import Input from "@/components/input";
import VerticallyCenter from "@/components/modal";

export default async function Page() {
    return (
        <main className="w-[inherit]">
            <Input />
            <VerticallyCenter />
        </main>
    );
}